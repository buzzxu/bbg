# {{projectName}} -- AI Coding Instructions

> Generated by bbg {{bbgVersion}} on {{date "YYYY-MM-DD"}}

This file provides instructions for Claude Code and other AI coding agents working with this project.

## Project

**{{projectName}}**: {{projectDescription}}

## Repositories

{{#each repos}}
- **{{name}}** ({{type}}): {{stack.language}}/{{stack.framework}} -- {{description}}
{{/each}}

## Core Principles

1. **Test-Driven** -- Write tests before implementation
2. **Security-First** -- Never hardcode secrets, validate all user inputs
3. **Immutability** -- Always create new objects, never mutate existing ones
4. **DRY** -- Extract shared logic, never duplicate functions
5. **Plan Before Execute** -- Understand the codebase before making changes
6. **Verify Continuously** -- Use `skills/verification-loop/SKILL.md` after every change

## Governance Resources

This project is equipped with comprehensive AI development governance:

- `agents/` -- Agent definitions for specialized tasks
- `skills/` -- Detailed workflow instructions (SKILL.md in each subdirectory)
- `rules/` -- Coding rules by language and category
- `commands/` -- Slash commands for AI tools
- `hooks/` -- Automation hooks for session lifecycle
- `contexts/` -- Operational mode switching (dev, review, research)
- `mcp-configs/` -- MCP server configurations

## Risk Thresholds

{{risk-table riskThresholds}}

## Must Always

- Run tests before committing
- Run build to verify compilation
- Follow conventional commits: `feat:`, `fix:`, `refactor:`, `docs:`, `test:`, `chore:`
- Write tests for new functionality
- Handle errors explicitly with try/catch

## Must Never

- Hardcode secrets, API keys, tokens, or absolute paths
- Submit untested changes
- Duplicate utility functions
- Mutate function arguments or shared state
- Silently swallow errors
- Skip type checking

## Security Guidelines

- No hardcoded secrets (API keys, passwords, tokens)
- Validate all user inputs at system boundaries
- Sanitize file paths (prevent path traversal)
- Use parameterized queries for database access
- Error messages must not leak sensitive data

## Detected Stack

- **Languages:** {{join languages ", "}}
- **Frameworks:** {{join frameworks ", "}}

{{#if-includes languages "typescript"}}
### TypeScript

- Use strict mode with ESM-only modules
- Use `.js` extensions for ESM imports
- Follow rules in `rules/typescript/`
{{/if-includes}}
{{#if-includes languages "java"}}
### Java

- Follow rules in `rules/java/`
- Use Spring conventions where applicable
{{/if-includes}}
{{#if-includes languages "python"}}
### Python

- Follow rules in `rules/python/`
- Use type hints for all public APIs
{{/if-includes}}
{{#if-includes languages "go"}}
### Go

- Follow rules in `rules/golang/`
- Use standard Go project layout
{{/if-includes}}
{{#if-includes languages "rust"}}
### Rust

- Follow rules in `rules/rust/`
- Use clippy for linting
{{/if-includes}}

{{#if-eq enableRedTeam true}}
## Red-Team Workflow

Red-team flow is enabled. See `docs/security/backend-red-team-playbook.md`.
{{/if-eq}}
{{#if-eq enableCrossAudit true}}
## Cross-Audit Workflow

Cross-audit flow is enabled. See `docs/workflows/cross-audit-policy.md`.
{{/if-eq}}
