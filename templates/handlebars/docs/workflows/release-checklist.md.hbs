# Release Checklist

Run this checklist for each release cut.

## Repository Set

{{#if repos.length}}
{{#each repos}}
- [ ] {{name}}
{{/each}}
{{else}}
- [ ] Confirm repository list is configured.
{{/if}}
