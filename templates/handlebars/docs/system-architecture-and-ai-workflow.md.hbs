# {{projectName}} System Architecture and AI Workflow

This workspace is generated by bbg and coordinates governance across repositories.

## Registered Repositories

{{#if repos.length}}
{{#each repos}}
- {{name}} ({{type}})
{{/each}}
{{else}}
- No repositories registered yet.
{{/if}}
